% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_cp_from_dates.R
\name{make_cp_from_dates}
\alias{make_cp_from_dates}
\title{make_cp_from_dates(): Counting process 자료 변환 함수
날짜 기반 원자료 → counting-process + time-dependent binary covariate 생성
\itemize{
\item 입력: landmark_days, accept, response, follow-up end, status
\item 출력: id, tstart, tstop, event, response 가 있는 data.frame
}}
\usage{
make_cp_from_dates(
  data,
  accept_col,
  tx_col,
  fu_col,
  status_col,
  eps = 0.00000001,
  fix_zero_followup = c("bump", "drop", "error")
)
}
\arguments{
\item{data}{분석할 data.frame}

\item{accept_col}{character. baseline 날짜 변수명 (예: "accept.dt").}

\item{tx_col}{character. 반응 날짜 변수명 (예: "tx.date").}

\item{fu_col}{character. 추적 종료(사망·중도절단 발생 시점) 날짜 변수명 (예: "fu.date").}

\item{status_col}{character. 사건 지표 변수명 (예: "fustat", 1=사망, 0=생존).}

\item{eps}{0-길이 구간을 피하기 위해 아주 작은 시간을 더해 주는 보정값.}

\item{fix_zero_followup}{futime==0(accept=fu 같은 날) 처리 방식(bump: 아주 살짝 늘림, drop: 해당 사례 제외, error: 중단)}

\item{landmark_days}{numeric 벡터. baseline(accept)로부터 며칠 후를 landmark로 지정 (예: c(60, 120)).}
}
\value{
data.frame. columns(id, tstart, tstop, event, response).
}
\description{
make_cp_from_dates(): Counting process 자료 변환 함수
날짜 기반 원자료 → counting-process + time-dependent binary covariate 생성
\itemize{
\item 입력: landmark_days, accept, response, follow-up end, status
\item 출력: id, tstart, tstop, event, response 가 있는 data.frame
}
}
