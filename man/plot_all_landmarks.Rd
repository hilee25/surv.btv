% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_all_landmarks.R
\name{plot_all_landmarks}
\alias{plot_all_landmarks}
\title{plot_all_landmarks(): 다중 랜드마크 민감도 분석 및 시각화 함수
여러 랜드마크 L들에 대해: (1) fit_for_L 결과/요약표 생성 + (2) KM 곡선/위험표 패널 그리드
\itemize{
\item 입력: cp_df, landmarks, 시각화 옵션
\item 처리: 각 L마다 fit_for_L + .survfit_at_L 수행
\item 출력: list(plot = patchwork grid, table = tibble 요약표, per_L = 내부 객체 목록)
}}
\usage{
plot_all_landmarks(
  cp_df,
  landmarks,
  robust = TRUE,
  conf_int = TRUE,
  tmax = NULL,
  palette = c("#1f77b4", "#d62728"),
  theme_fn = ggplot2::theme_bw,
  base_size = 12,
  legend_position = "top",
  ncol = 2,
  title_prefix = "Landmark L = ",
  show_titles = TRUE,
  show_risktable = TRUE,
  risk_table_height = 0.35,
  risk_table_base_size = 10,
  annotate_hrci = TRUE,
  annotate_mb_p = TRUE,
  annotate_cox_p = FALSE,
  ref_label = "Z=0"
)
}
\arguments{
\item{cp_df}{data.frame(id, tstart, tstop, event, response).}

\item{landmarks}{numeric 또는 길이 1 이상의 벡터.}

\item{robust}{cluster(id) 기반 강건 분산 사용 여부(기본 TRUE).}

\item{conf_int}{KM 곡선의 신뢰구간 표시 여부(기본 TRUE).}

\item{tmax}{모든 패널에서 공통 x축 상한(미지정 시 자동).}

\item{palette}{라인 팔레트 (길이 2 권장).}

\item{theme_fn}{ggplot2 테마 함수. (기본 ggplot2::theme_bw).}

\item{base_size}{메인 생존곡선 패널의 기본 글자 크기.}

\item{legend_position}{범례 위치(기본 top).}

\item{ncol}{patchwork   그리드에서 한 행에 배치할 패널 수(기본 2).}

\item{title_prefix}{패널 제목에 사용할 접두 문자열.}

\item{show_titles}{각 랜드마크 패널에 제목 표시 여부(기본 TRUE).}

\item{show_risktable}{각 패널 하단에 위험표 표시 여부(기본 TRUE).}

\item{risk_table_height}{위험표가 패널 전체 높이에서 차지할 비율.}

\item{risk_table_base_size}{위험표의 기본 글자 크기.}

\item{annotate_hrci}{HR과 95\% CI를 주석으로 표시할지 여부(기본 TRUE).}

\item{annotate_mb_p}{Mantel–Byar p를 주석으로 표시할지 여부(기본 TRUE).}

\item{annotate_cox_p}{Cox score p를 주석으로 표시할지 여부(기본 FALSE).}

\item{ref_label}{참조군으로 간주할 Z 레이블 (예: "Z=0").}
}
\value{
list(plot = patchwork plot, table = tibble 요약표, per_L = 각 L의 객체 리스트)
}
\description{
plot_all_landmarks(): 다중 랜드마크 민감도 분석 및 시각화 함수
여러 랜드마크 L들에 대해: (1) fit_for_L 결과/요약표 생성 + (2) KM 곡선/위험표 패널 그리드
\itemize{
\item 입력: cp_df, landmarks, 시각화 옵션
\item 처리: 각 L마다 fit_for_L + .survfit_at_L 수행
\item 출력: list(plot = patchwork grid, table = tibble 요약표, per_L = 내부 객체 목록)
}
}
