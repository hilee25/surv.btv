% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mantel_byar_cp.R
\name{mantel_byar_cp}
\alias{mantel_byar_cp}
\title{mantel_byar_cp(): Mantel-Byar 검정 함수
각 사건 시점에서 위험집단을 재구성하여 Mantel-Byar 검정을 수행
\itemize{
\item 입력: counting-process 데이터(id, tstart, tstop, event, response), ref_val(기준 상태 값)
\item 처리: 사건 발생 시점들의 위험집단에서 계산
\item 출력: list(chisq, p, D_ref, E, V, contrib(시점별 표), ref_val)
}}
\usage{
mantel_byar_cp(cp, ref_val = 0L)
}
\arguments{
\item{cp}{data.frame. columns(id, tstart, tstop, event, response).}

\item{ref_val}{0 또는 1. 통상 0을 참조군으로 둠.}
}
\value{
list(chisq, p, D_ref, E, V, contrib, ref_val)
}
\description{
mantel_byar_cp(): Mantel-Byar 검정 함수
각 사건 시점에서 위험집단을 재구성하여 Mantel-Byar 검정을 수행
\itemize{
\item 입력: counting-process 데이터(id, tstart, tstop, event, response), ref_val(기준 상태 값)
\item 처리: 사건 발생 시점들의 위험집단에서 계산
\item 출력: list(chisq, p, D_ref, E, V, contrib(시점별 표), ref_val)
}
}
